apiVersion: v1
kind: Service
metadata:
  labels:
    app: "{{ .Values.thehive.app }}"
  name: "{{ .Values.thehive.name }}"
spec:
  type: NodePort
  ports:
    - port: 9000
      nodePort: 30001
  selector:
    app: "{{ .Values.thehive.app }}"